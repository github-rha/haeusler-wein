source "https://rubygems.org"

# Matches the GitHub Pages build environment (Jekyll + supported plugins).
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer bundles webrick; needed for `jekyll serve`.
gem "webrick", "~> 1.8"
